"""Training loop primitives.

* :mod:`sasrec_injection.training.losses`           — BCE next-item loss.
* :mod:`sasrec_injection.training.trainer`          — :class:`BaseTrainer`,
                                             :class:`SASRecTrainer` (P1
                                             baseline).
* :mod:`sasrec_injection.training.item_table_trainer`  — :class:`ItemTableTrainer`,
                                             which is BaseTrainer + the
                                             InfoNCE alignment loss.
"""

__all__: list[str] = []
